(ns collection-json.http )

(def options {:headers { "Accept" "application/vnd.collection+json,application/json;q=0.7,*/*;q=0.2"}})

(defn follow [link] nil)

(defn create [href template] nil)

(defn update [href template] nil)

(defn delete [href] nil)